extends layout

block content
    h1= "Questions that " + found.username+ " got incorrect: " + found.incorrect.length
    each question in found.incorrect
        a(href = "/random/"+question.id)
            li=question.id