extends layout

block content
    h1= "Questions that " + found.username+ " passed: " + found.passed.length
    each question in found.passed
        a(href = "/random/"+question.id)
            li=question.id